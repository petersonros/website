/** URL canônica do site, publicado no domínio próprio via GitHub Pages. */
export const SITE_URL = "https://petersonros.com";

/** Rotas estáticas conhecidas (sem depender de API externa). */
export const STATIC_ROUTES = [
  "/",
  "/sobre/",
  "/formacao/",
  "/projetos/",
  "/tutoriais/",
  "/scratch/",
] as const;
